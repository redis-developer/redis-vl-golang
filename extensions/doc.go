// Package extensions groups the RedisVL AI extensions: cache
// (EmbeddingsCache, SemanticCache), history (MessageHistory,
// SemanticMessageHistory), router (SemanticRouter), and vectorize (the
// Vectorizer interface). See the subpackages for the actual APIs; this
// package only anchors shared documentation and integration tests.
package extensions
